header#header
    h1= title
    button#refresh 새로고침
    script.
        (function () {
            document.getElementById('refresh').addEventListener('click', function (event) {
                $.post(
                    '/refresh',
                    {},
                    function () {

                    }
                )
            });
        })();
